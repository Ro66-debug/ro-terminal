{
  "name": "ro-terminal",
  "version": "0.2.0",
  "private": true,
  "description": "Ro Terminal — Bybit USDT perpetuals board, static site + 5-minute data snapshot.",
  "scripts": {
    "start": "node server.js",
    "fetch": "node scripts/fetch-bybit.js",
    "test": "playwright test",
    "test:live": "playwright test tests/live.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0"
  }
}
